<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A bare splash theme only. Every visual decision is made in Compose, so the XML theme
      exists to avoid a white flash before the first frame and nothing else.
    -->
    <style name="Theme.SonoRitmo" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/background_light</item>
    </style>
</resources>
